# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Never Forget' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'Eureka', :git => 'https://github.com/xmartlabs/Eureka.git', :branch => 'Swift3'
  pod 'RealmSwift'
  pod 'FontAwesome.swift', :git => 'https://github.com/thii/FontAwesome.swift', :tag => '0.11.1'
  
  # Pods for Never Forget

  target 'Never ForgetTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Never ForgetUITests' do
    inherit! :search_paths
    # Pods for testing
  end
end
